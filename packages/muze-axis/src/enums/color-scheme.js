/**
 * This file is used to export the default color schemes supported by d3
 * and keeping the module agnostic to d3.
 */
import { SCHEMES } from '../scale-creator';

export const SCHEME1 = SCHEMES.SCHEME1;
export const SCHEME2 = SCHEMES.SCHEME2;
export const SCHEME3 = SCHEMES.SCHEME3;
