/**
 * ProjectionMode enum defines the projection modes performed on DataModel during field projection.
 *
 * @readonly
 * @enum {string}
 */
const ProjectionMode = {
    INCLUDE: 'include',
    EXCLUDE: 'exclude',
};

export default ProjectionMode;
